import * as React from 'react';
import { AppContainer } from './App.styles';

function App() {
  return (
    <React.Fragment>
      <AppContainer>
        Base for React TypeScript app with eslint and prettier.
      </AppContainer>
    </React.Fragment>
  );
}

export default App;
